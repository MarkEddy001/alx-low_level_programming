#include <stdio.h>

/**
 *main - main block
 *printf(with proper grammar, but the outcome is a piece of art,\n)
 *Return: Always 0(Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
